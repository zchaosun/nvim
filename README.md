## 环境准备
```shell
brew tap homebrew/cask-fonts
brew install --cask font-hack-nerd-font

brew tap daipeihust/tap
brew install im-select

brew python stylua node
pip3 install neovim

v ~/.zshrc
alias v='nvim'
alias vim='nvim'
```